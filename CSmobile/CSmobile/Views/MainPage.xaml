<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CSmobile"
             x:Class="CSmobile.MainPage">

    <ContentPage.Content>
        <ScrollView>
            <StackLayout x:Name="mainmenu">
                
                <Label Text="Menu" FontSize="16"
                Margin="0, 30, 0, 0"
                HorizontalOptions="Center" />

                <Button Text="Tickets"
                Margin="0,30,0,0"
                Clicked="Tickets"/>

                <Button Text="Contacts" 
                Margin="0,-10,0,0"
                Clicked="Contacts"/>

                <Button Text="Companies"  
                Margin="0,-10,0,0"
                Clicked="Companies"/>

                <Button Text="Tasks" 
                Margin="0,-10,0,0"
                Clicked="Tasks"/>

                <Button Text="Document Search"
                 Margin="0,-10,0,0"
                 Clicked="DocumentSearch"/>

                <Button Text="Logout" 
                Margin="0,-10,0,0"
                Clicked="Logout"/>
                
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>

